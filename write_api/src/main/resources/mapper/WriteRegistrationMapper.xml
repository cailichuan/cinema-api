<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cinema.write_api.mapper.WriteRegistrationMapperr">

    <insert id="insert" parameterType="Registration">
        inset into cinema.t_registration
        (id,aid,uid,createAt)
        values
        (#{id},#{aid},#{uid},#{createAt})
    </insert>
</mapper>